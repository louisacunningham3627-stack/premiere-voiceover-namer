# 0.4.0 安装后测试记录

## 0.4.4 占用等待状态修复

- 删除 `executeCandidate` 提前调用 `setJobError` 的行为，自动重试由 `advancePending` 分类后设置状态；手动扫描的失败仍由其外层显示。
- 自动占用分支显示“等待文件释放”，保留 pending.plan 与稳定计数；存在 rollbackWarnings 时不隐藏错误。每条录音沿用独立 nextAttemptAt，锁重试返回后扫描继续处理后续录音。
- 189 项测试、构建校验、Windows 28 / Mac 22 项包校验通过；保存测试工程并正常退出后安装 0.4.4，21 项安装校验通过。已发起重开相同工程。
- 旧版备份后缀 `before-20260906-040152-ea2cac3e`。桌面工具复测时报 `Computer Use helper already has an active request`，未抢占其它操作；当前只能确认安装和启动，未确认新版面板或真实占用状态。未删除录音，实际时延仍待测。

## 0.4.3 占用重试修复

- 2026-09-06：统一文件锁识别，`failureDisposition` 可将无 code 的 `resource busy or locked`、中文占用和包装 cause 分类为 `retry-file`；取消和上下文歧义仍优先处理。
- 锁分支保留稳定计数及预留名称，不进入红色错误分支；原有 500ms 起步退避按 1200ms 扫描节奏执行。首次 3000ms 写入稳定保护和实际写入变化后的重新检查不变，不承诺即刻改名。
- 188 项测试、构建校验及 Windows 28 / Mac 22 项包校验通过。通过 Computer Use 保存当前临时工程（标题星号消失），正常退出、安装、重开并打开面板。安装 21 项校验通过，真实面板显示 0.4.3、绿色自动待命。
- 原版备份：`C:\Users\Administrator\AppData\Roaming\Adobe\UXP\PluginBackups\com.hechao.premiere.voiceover-namer-before-20260906-035406-d285eabd`。未强杀进程，未删除录音。尚未实测新一轮录音锁释放延迟，未验证回收闭环。

日期：2026-09-06。

## 已通过

- 在 Premiere 关闭时安装 0.4.0，构建与安装目录的 21 个发布文件逐项 SHA-256 一致。
- 旧 0.3.4 安装已备份到 `C:\Users\Administrator\AppData\Roaming\Adobe\UXP\PluginBackups\com.hechao.premiere.voiceover-namer-before-20260906-025913-32b8c674`。
- 当前用户的专用协议已注册，通信数据位于插件代码目录之外。
- 通过已安装的 URL 协议唤起真实助手，完成准备、最终确认及回收响应。
- 只回收一个专门生成的 46 字节 WAV；在系统回收站按原目录、原文件名及 SHA-256 确认其内容。未删除正式素材。
- 证据为 `work/installed-bridge-test-gAhvcU/验证结果.json`；复测脚本为 `scripts/test-installed-recycle-bridge.mjs`。凭据仅在助手运行数据目录中使用，不进入报告。

## 宿主测试准备

- 旧测试工程存在旧素材引用，不用于此次删除测试。
- 使用本机 Premiere 随附的中文 Standard Template Project 创建独立副本，结构化读取确认外部媒体引用为 0。
- 测试工程：`work/runtime-smoke-20260906-030541/录音命名与回收-临时验收.prproj`。
- 后续用户截图确认已进入工程编辑界面，工程保存为 `录音命名与回收-临时验收_1.prproj`；此前 `OS_PopupWindow` 阻塞已解除。
- 0.4.0 面板已加载，但自动布防在创建工程同级媒体目录时失败。父目录 ACL 允许当前账户写入，具体 UXP 异常被旧代码合并为笼统提示，底层原因尚未确认。
- 0.4.1 候选改用 UXP `localFileSystem.getEntryWithUrl` 获取父目录，再用 `Folder.createFolder` 创建单层目录并读回验证；保留 fs 兼容回退及两接口错误详情。没有改工程输出层级、磁盘权限、工程文件或录音。
- 没有使用 Computer Use。Premiere 运行期间不覆盖安装版；候选须在用户正常关闭 Premiere 后安装并复测。
- 0.4.1 候选验证：184 项 Node 测试、构建文件校验、Windows 28 项及 Mac 22 项文件哈希和 ZIP 读回全部通过；这些不替代真实 UXP 目录创建验证。安装版未修改，无需回滚；旧版本包仍保留。

## 尚未通过

- 0.4.1 在 Premiere 内自动创建目录、原生画外音录制、新录音登记及速度实测。
- 时间线删除、30 秒内撤销、跨序列引用保护与回收后项目项移除的真实宿主闭环。

原生助手测试通过不等于以上 Premiere 内交互已通过，`runtime_tested=false` 保持。

## 0.4.2 实机修复复测

- 用户要求代为操作，不再把保存、退出和重开步骤交回用户。通过 Computer Use 查看并操作本测试工程；首次保存/退出期间检测到用户输入，最终只读确认工程于 03:34:41 落盘且 PR 已退出。未强制终止进程。
- 0.4.1 安装 21 项校验通过，但实机仍报 `Could not find an entry of file:///`；面板版本文字误留 0.4.0。该候选不能算修复成功。
- 0.4.2 在 Core 路径拆分、目标推导、比较中统一剥离 Windows 扩展路径前缀 `\\?\`，UNC 扩展前缀转换为普通 UNC；补充面板/manifest 版本一致测试。186 项测试与双平台包校验通过。
- 通过 PR 原生关闭按钮正常退出后安装 0.4.2，21 个发布文件安装校验通过，重新打开同一 `_1.prproj`。真实面板显示 `0.4.2`、绿色“自动待命”、主状态“直接点音轨麦克风录音”，原目录错误已消失。
- 工程同级 `Adobe Premiere Pro Captured and Generated` 可访问，原 `音频 1.wav`（1462518 字节）仍保留。该目录在升级前已由录音流程生成，本轮证明目录识别/布防恢复，不把其存在误算成插件首次新建目录的证据。
- 本轮没有录制新音频或测试删除。全流程 `runtime_tested=false` 保持；扩展路径在回收平台门和媒体源 API 的其它消费点仍需后续专项验证。
- 当前安装 0.4.2。恢复到本轮升级前 0.4.0 的备份：`C:\Users\Administrator\AppData\Roaming\Adobe\UXP\PluginBackups\com.hechao.premiere.voiceover-namer-before-20260906-033527-65fb442e`。0.4.1 的中间备份保存在 `com.hechao.premiere.voiceover-namer-before-20260906-034045-840ec461`。
